{
  "name": "booxtra-agents",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=20.0.0" },
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/validate.test.mjs",
    "build": "bash scripts/build.sh"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
